required_r_packages <- c(
  "Rcpp", "digest", "readxl",
  "GeneralizedHyperbolic", "jsonlite", "arrow", "Bessel", "GIGrvg",
  "tseries"
)

required_python_packages <- c("numpy", "pandas", "pyarrow", "scipy")
