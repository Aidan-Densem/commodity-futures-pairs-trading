# Validation entry points

Validation is currently exercised through `tests/smoke/run_smoke_tests.R` and the
stage-specific checks documented in `docs/VALIDATION.md`. This directory is
reserved for future empirical validation entry points; it contains no competing
production pipeline.
